// api/ensembleApi.ts
import axiosInstance from './axiosInstance';

/*export const createEnsemble = async (data: EnsembleData) => {
  const response = await axiosInstance.post('/ensembles', data);
  return response.data;
};

export const getEnsembles = async () => {
  const response = await axiosInstance.get('/ensembles');
  return response.data;
};*/
